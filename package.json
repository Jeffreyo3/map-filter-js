{
  "name": "map-filter-js",
  "author": "jeffreyo3",
  "version": "1.0.4",
  "license": "MIT",
  "description": "A utility that will filter and possibly transform in a single iteration of an array",
  "type": "module",
  "devDependencies": {
    "@types/jest": "^29.5.1",
    "@types/node": "^20.1.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  },
  "jest": {
    "verbose": true,
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": [
      "**/*.spec.ts"
    ]
  },
  "scripts": {
    "build": "tsc",
    "build:cjs": "tsc -p tsconfig.cjs.json",
    "start": "node dist/index.js",
    "prepublishOnly": "npm run build",
    "test": "jest --no-cache --coverage --testPathPattern=src/",
    "test:watch": "jest --watchAll --testPathPattern=src/",
    "test:bench": "npm run build && npm run build:cjs && jest --no-cache --testPathPattern=benchmarks/"
  },
  "main": "dist/index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Jeffreyo3/map-filter-js.git"
  },
  "bugs": {
    "url": "https://github.com/Jeffreyo3/map-filter-js/issues"
  },
  "homepage": "https://github.com/Jeffreyo3/map-filter-js#readme"
}
